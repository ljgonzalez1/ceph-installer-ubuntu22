ask_for_node_type() {
    # Ask user for node type.
    # Tell user to first configure all the secondary nodes, the configure the primary nodes because of the certificates, which needs to be copied from the primary to the secondaries.
    # 
    # ( ) Pimary (Manager node)
    # ( ) Secondary {default preselected}


    # < > Ok
    # < > Cancel (stops de script with error)
    true
}
